version: "3.9"
services:
  persona-ai:
    build: .
    container_name: persona-ai
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./app/logs:/app/logs
    networks:
      - persona-net

networks:
  persona-net:
    driver: bridge
